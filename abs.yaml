aggregate_check: False

upload_without_merge: True

upload_to_staging_channel_in_pr: False

channels: [cuda12_def]

# don't skip existing
build_parameters:
  - "--suppress-variables"
  - "--error-overlinking"
