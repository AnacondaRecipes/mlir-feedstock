# Use clang_bootstrap for macOS self-hosted builds
c_compiler:
  - clang_bootstrap     # [osx]
cxx_compiler:
  - clang_bootstrap     # [osx]

# Build with clang 22 compiler
c_compiler_version:
  - 22.1.2              # [osx]
cxx_compiler_version:
  - 22.1.2              # [osx]
